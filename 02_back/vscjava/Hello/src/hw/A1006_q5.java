package hw;

public class A1006_q5 {
  public static void main(String[] args) {
    // 문제 5: 2차원 배열 합치기
    // 두 개의 2차원 배열을 입력으로 받아, 두 배열을 가로로 합치는 프로그램을 작성하세요.
    int[][] array1 = { { 1, 2 }, { 3, 4 } };
    int[][] array2 = { { 5, 6 }, { 7, 8 } };



  }
}

/* 
mergeArrays 함수가 가로로 합치는 것은 두 개의 2차원 배열을 가로로 연결하여 새로운 2차원 배열을 만드는 작업을 의미합니다. 이것은 다음과 같은 상황에서 유용할 수 있습니다:

두 개의 2차원 배열이 있고, 각 배열의 행 수는 같지만 열 수가 다를 때, 두 배열을 가로로 합쳐서 더 큰 배열을 만들 수 있습니다.

두 개의 2차원 배열을 가로로 합치면, 각 배열의 행은 그대로 유지되고 열이 연결됩니다. 이렇게 합쳐진 배열은 두 배열의 데이터를 하나로 결합한 것과 같습니다.

예를 들어, 다음과 같은 두 개의 2차원 배열이 있다고 가정해 봅시다:

java
Copy code
int[][] array1 = {{1, 2}, {3, 4}};
int[][] array2 = {{5, 6}, {7, 8}};
이 두 배열을 mergeArrays 함수를 사용하여 가로로 합치면 다음과 같은 결과가 나옵니다:

java
Copy code
int[][] mergedArray = mergeArrays(array1, array2);
mergedArray는 다음과 같은 형태를 가집니다:

Copy code
{{1, 2, 5, 6}, {3, 4, 7, 8}}
즉, 두 배열의 행은 그대로 유지되고, 열이 연결되어 새로운 2차원 배열이 생성됩니다. 이렇게 가로로 합치면 두 배열의 데이터를 결합하여 사용할 수 있습니다.
 */


/* 
두 개의 2차원 배열을 "세로로 합치는 것"은 두 배열을 위 아래로 연결하여 새로운 2차원 배열을 만드는 작업을 의미합니다. 이것은 다음과 같은 상황에서 유용할 수 있습니다:

두 개의 2차원 배열이 있고, 각 배열의 열 수는 같지만 행 수가 다를 때, 두 배열을 세로로 합쳐서 더 큰 배열을 만들 수 있습니다.

두 개의 2차원 배열을 세로로 합치면, 각 배열의 열은 그대로 유지되고 행이 연결됩니다. 이렇게 합쳐진 배열은 두 배열의 데이터를 하나로 결합한 것과 같습니다.

예를 들어, 다음과 같은 두 개의 2차원 배열이 있다고 가정해 봅시다:

java
Copy code
int[][] array1 = {{1, 2}, {3, 4}};
int[][] array2 = {{5, 6}, {7, 8}};
이 두 배열을 세로로 합치면 다음과 같은 결과가 나옵니다:

java
Copy code
int[][] mergedArray = mergeArrays(array1, array2);
mergedArray는 다음과 같은 형태를 가집니다:

Copy code
{{1, 2}, {3, 4}, {5, 6}, {7, 8}}
즉, 두 배열의 열은 그대로 유지되고, 행이 연결되어 새로운 2차원 배열이 생성됩니다. 이렇게 세로로 합치면 두 배열의 데이터를 결합하여 사용할 수 있습니다.
 */